export type ConnexionTypes = {
    // [key: string]: string | number | Date | undefined | any
    id: string,
    key: string,
    start: string,
    stop: string,
    subscribed: boolean,
    inactive: boolean
}

