export type PaymentTypes = {
    // amount: number,
    // id: string,
    // targetId: string,
    // type: string,
    // userId: string | undefined,
    // inactive: string | boolean,
    customerId: string,
    // rest: any
    id: string,
    targetId: string,
    inactive: boolean,
    type: string,
    rest: number,
    createdAt: string,
    amount: number,
}